import { createTheme } from "@mui/material/styles";

const btnTheme = createTheme({
  palette: {
    primary: {
      main: "#7868E6",
    },
    secondary: {
      main: "#B5F4FF",
    },
  },
});

export default btnTheme;
