import { createTheme } from "@mui/material/styles";

const btnTheme = createTheme({
  palette: {
    primary: {
      main: "#5e63b6",
    },
    secondary: {
      main: "#11cb5f",
    },
  },
});

export default btnTheme;
