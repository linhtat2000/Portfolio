import { createTheme } from "@mui/material/styles";

const btnTheme = createTheme({
  palette: {
    primary: {
      main: "#5e63b6",
    },
    secondary: {
      main: "#7868E6",
    },
  },
});

export default btnTheme;
